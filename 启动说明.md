# NEP 项目本地启动说明

---

## 目录

- [📋 环境要求](#环境要求)
- [🗄️ 步骤一：初始化数据库](#步骤一初始化数据库)
- [⚙️ 步骤二：启动后端（Spring Boot）](#步骤二启动后端spring-boot)
- [🖥️ 步骤三：启动管理端前端（Vue 3）](#步骤三启动管理端前端vue-3)
- [📊 步骤四：启动决策者端大屏（Vue 3）](#步骤四启动决策者端大屏vue-3)
- [🌐 步骤五：访问应用](#步骤五访问应用)
- [🔌 端口一览](#端口一览)

---

## 环境要求

| 依赖 | 版本 | 说明 / 路径 |
| :--- | :--- | :--- |
| **Java (JDK)** | 17 | `C:\Program Files\Java\jdk-17` |
| **MySQL** | 8.x | 本地服务，默认端口 **3306** |
| **Node.js** | ≥ 22 | `D:\nodejs`（系统 PATH 默认 v18，启动前端前需手动切换） |

> 💡 **提示**：建议提前将 JDK 17 和 Node 22 加入环境变量，避免每次手动设置。

---

## 步骤一：初始化数据库

确保 MySQL 服务已启动，并创建/检查数据库 `nep`。

### 1.1 检查数据库是否存在

```sql
mysql -u root -p123456 -e "SHOW DATABASES LIKE 'nep';"
```

### 1.2 初始化数据库（若不存在）

执行初始化脚本，建表并导入基础数据：

```bash
mysql -u root -p123456 < nep-backend/src/main/resources/sql/init.sql
```

---

## 步骤二：启动后端（Spring Boot）

服务端口：**8080**

打开一个新终端，执行：

```cmd
:: 进入后端目录
cd C:\Users\Camellia\Desktop\东软实训\neuesoft\DNUI-project-1\nep-backend

:: 指定 JDK 17（如已全局配置可跳过）
set JAVA_HOME=C:\Program Files\Java\jdk-17

:: 启动 Spring Boot 应用
mvn spring-boot:run
```

### ✅ 启动成功标志

控制台输出：

```
Started NepApplication in X seconds
```

### 🔑 默认管理员账号

| 账号 | 密码 |
| :--- | :--- |
| `admin` | `123` |

---

## 步骤三：启动管理端前端（Vue 3）

服务端口：**8083**

打开**另一个**新终端，执行：

```cmd
:: 进入管理端前端目录
cd C:\Users\Camellia\Desktop\东软实训\neuesoft\DNUI-project-1\nep-frontend-nepm

:: 切换到 Node.js 22（否则 Vite 8 会报错，如已全局配置可跳过）
set PATH=D:\nodejs;%PATH%

:: 首次运行需安装依赖（后续启动无需重复）
npm install

:: 启动开发服务器
npm run dev
```

### ✅ 启动成功标志

控制台输出：

```
  ➜  Local:   http://localhost:8083/
```

---

## 步骤四：启动决策者端大屏（Vue 3）

服务端口：**8084**

打开**另一个**新终端，执行：

```cmd
:: 进入决策者端前端目录
cd C:\Users\Camellia\Desktop\东软实训\neuesoft\DNUI-project-1\nep-frontend-nepv

:: 切换到 Node.js 22（否则 Vite 8 会报错，如已全局配置可跳过）
set PATH=D:\nodejs;%PATH%

:: 首次运行需安装依赖（后续启动无需重复）
npm install

:: 启动开发服务器
npm run dev
```

### ✅ 启动成功标志

控制台输出：

```
  ➜  Local:   http://localhost:8084/
```

### 决策者端功能

大屏可视化展示，包含首页仪表盘及数据统计决策面板。

---

## 步骤五：访问应用

打开浏览器，访问：

> 🌐 **http://localhost:8083**

使用以下凭据登录管理端：

| 账号 | 密码 |
| :--- | :--- |
| `admin` | `123` |

---

## 端口一览

| 服务 | 端口 | 项目路径 | 说明 |
| :--- | :---: | :--- | :--- |
| 🟢 后端 Spring Boot | **8080** | `nep-backend/` | API 接口服务 |
| 🟡 管理端 NEPM | **8083** | `nep-frontend-nepm/` | 系统管理员操作界面 |
| 🟣 决策者端 NEPV | **8084** | `nep-frontend-nepv/` | 决策者大屏可视化 |
| ⚪ 监督员端 NEPS | 8081 | `nep-frontend-neps/` | （如已构建，公众监督员端） |

---

> ⚠️ **注意**：启动顺序建议为「数据库 → 后端 → 前端」，否则前端可能因后端未就绪而报错。
